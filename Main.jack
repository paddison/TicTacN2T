class Main {
    
    function void main() {
        var boolean tests;
        var Game g;
        let tests = false;
        if (tests) {
            do Test.run();
        }
        else { 
            let g = Game.new();
            do g.init();
        }
        
        return;
    }
}